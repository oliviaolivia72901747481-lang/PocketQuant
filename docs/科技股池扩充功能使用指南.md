# 科技股池扩充功能使用指南

> 本指南帮助新手快速了解和使用科技股池扩充系统的新增功能

---

## 📋 目录

1. [功能概述](#功能概述)
2. [新增功能一览](#新增功能一览)
3. [快速上手](#快速上手)
4. [核心功能使用](#核心功能使用)
5. [数据质量保障](#数据质量保障)
6. [常见操作示例](#常见操作示例)
7. [注意事项](#注意事项)

---

## 功能概述

### 科技股池扩充了什么？

原有科技股池仅包含27只股票，现已扩充至80-100只高质量科技股，覆盖8个科技细分行业：

| 行业 | 代表性公司 | 股票数量 |
|------|------------|:--------:|
| 半导体 | 中芯国际、韦尔股份 | 15-20只 |
| 人工智能 | 科大讯飞、海康威视 | 10-15只 |
| 5G通信 | 中兴通讯、烽火通信 | 8-12只 |
| 新能源科技 | 宁德时代、隆基绿能 | 12-15只 |
| 消费电子 | 立讯精密、歌尔股份 | 8-10只 |
| 软件服务 | 用友网络、金山办公 | 10-12只 |
| 生物医药科技 | 迈瑞医疗、药明康德 | 8-10只 |
| 智能制造 | 汇川技术、埃斯顿 | 8-10只 |

### 新增了哪些能力？

1. **智能筛选引擎** - 自动从全市场筛选科技股
2. **多维度评分系统** - 财务、成长、市场、竞争力综合评估
3. **多源数据交叉验证** - 确保数据准确可靠
4. **风险控制机制** - 行业集中度、个股权重自动监控
5. **动态更新机制** - 支持定期自动更新股票池

---

## 新增功能一览

### 🔍 股票筛选功能

| 功能 | 说明 | 使用场景 |
|------|------|----------|
| 行业筛选 | 基于关键词匹配科技行业 | 发现新的科技股 |
| 财务筛选 | ROE、负债率等指标过滤 | 筛选财务健康的公司 |
| 市场筛选 | 市值、流动性、波动率过滤 | 确保可交易性 |
| 综合评分 | 多维度加权评分 | 排序和选择最优股票 |

### 📊 数据质量功能

| 功能 | 说明 | 使用场景 |
|------|------|----------|
| 多源数据获取 | 支持akshare、东方财富等 | 数据源故障时自动切换 |
| 交叉验证 | 多数据源对比验证 | 确保数据准确性 |
| 数据准确率检测 | 自动检测数据异常 | 发现数据问题 |
| 本地备份 | 自动备份历史数据 | 数据恢复 |

### ⚠️ 风险控制功能

| 功能 | 说明 | 使用场景 |
|------|------|----------|
| 行业集中度监控 | 单一行业不超过25% | 分散行业风险 |
| 个股权重监控 | 单只股票不超过5% | 分散个股风险 |
| 风险预警 | 自动识别高风险股票 | 提前规避风险 |

---

## 快速上手

### 第一步：查看扩充后的股票池

```python
# 在Python中查看
from config.tech_stock_pool import get_tech_stock_pool

pool = get_tech_stock_pool()
stocks = pool.get_all_stocks()

print(f"股票池总数: {len(stocks)}")
for stock in stocks[:10]:  # 显示前10只
    print(f"{stock.code} {stock.name} - {stock.sector}")
```

### 第二步：运行股票筛选

```bash
# 命令行运行筛选工具
python tools/tech_stock_pool_expander.py
```

### 第三步：查看筛选结果

筛选完成后，查看以下文件：
- `data/tech_stock_candidates.csv` - 候选股票列表
- `config/tech_stock_pool.py` - 更新后的股票池

---

## 核心功能使用

### 1. 使用多源数据交叉验证

这是本次新增的重要功能，用于确保数据准确性。

```python
from core.stock_screener import cross_validate_sources
import pandas as pd

# 假设你有两个数据源的数据
source1_df = pd.DataFrame({
    'code': ['000001', '000002', '600000'],
    'name': ['平安银行', '万科A', '浦发银行'],
    'price': [10.0, 20.0, 15.0],
    'total_market_cap': [1000e8, 2000e8, 1500e8],
})

source2_df = pd.DataFrame({
    'code': ['000001', '000002', '600000'],
    'name': ['平安银行', '万科A', '浦发银行'],
    'price': [10.1, 20.2, 15.1],  # 略有差异
    'total_market_cap': [1010e8, 2020e8, 1510e8],
})

# 执行交叉验证
report = cross_validate_sources(
    source1_df, 
    source2_df, 
    source1_name="akshare", 
    source2_name="eastmoney"
)

# 查看验证结果
print(f"验证状态: {report.status.value}")
print(f"总体匹配率: {report.overall_match_rate:.2f}%")
print(f"是否通过: {'是' if report.is_valid else '否'}")

# 查看详细字段匹配情况
for field_result in report.field_results:
    print(f"  {field_result.field_name}: {field_result.match_rate:.2f}%")
```

### 2. 使用数据源管理器

```python
from core.stock_screener import get_data_source_manager

# 获取数据源管理器
manager = get_data_source_manager()

# 获取主板和中小板股票
result = manager.get_mainboard_stocks()

if result.success:
    print(f"获取到 {len(result.data)} 只股票")
    print(f"数据来源: {result.source.value}")
else:
    print(f"获取失败: {result.error_message}")

# 查看数据源健康状态
health = manager.check_health()
print(f"整体健康: {health['overall_healthy']}")
for source, status in health['sources'].items():
    print(f"  {source}: {'健康' if status['healthy'] else '异常'}")
```

### 3. 使用综合评分系统

```python
from core.stock_screener import get_comprehensive_scorer

scorer = get_comprehensive_scorer()

# 对单只股票评分（需要提供股票数据）
stock_data = {
    'code': '000001',
    'name': '平安银行',
    'roe': 12.5,
    'debt_ratio': 45.0,
    'revenue_growth_3y': 15.0,
    'profit_growth_3y': 18.0,
    'total_market_cap': 2000e8,
    'turnover': 10e8,
    'volatility': 25.0,
}

score = scorer.score_stock(stock_data)

print(f"综合得分: {score.total_score:.1f}")
print(f"评级: {score.rating}")
print(f"财务得分: {score.financial_score:.1f}")
print(f"成长得分: {score.growth_score:.1f}")
print(f"市场得分: {score.market_score:.1f}")
print(f"优势: {score.strengths}")
print(f"劣势: {score.weaknesses}")
```

### 4. 使用风险评估

```python
from core.stock_screener import get_risk_assessor

assessor = get_risk_assessor()

# 评估股票池风险
pool_data = [
    {'code': '000001', 'industry': '半导体', 'weight': 0.05},
    {'code': '000002', 'industry': '半导体', 'weight': 0.05},
    {'code': '600000', 'industry': '人工智能', 'weight': 0.04},
    # ... 更多股票
]

result = assessor.assess_pool_risk(pool_data)

print(f"风险等级: {result.risk_level.value}")
print(f"行业集中度: {result.metrics.industry_concentration:.1f}%")
print(f"最大单股权重: {result.metrics.max_stock_weight:.1f}%")

# 查看风险预警
for warning in result.warnings:
    print(f"⚠️ {warning.message}")
```

---

## 数据质量保障

### 数据准确率验证

系统会自动验证数据准确率，目标是达到99%以上：

```python
from core.stock_screener import validate_data_accuracy

# 验证数据准确率
report = validate_data_accuracy(stock_df)

print(f"总体准确率: {report.overall_accuracy:.2f}%")
print(f"准确率等级: {report.accuracy_level.value}")
print(f"是否达标: {'是' if report.meets_target else '否'}")

# 查看各字段准确率
for field_result in report.field_results:
    status = "✓" if field_result.is_accurate else "✗"
    print(f"  {status} {field_result.field_name}: {field_result.accuracy_rate:.2f}%")
```

### 数据备份与恢复

系统会自动备份数据，支持故障恢复：

```python
from core.stock_screener import get_backup_manager

backup_mgr = get_backup_manager()

# 查看备份状态
summary = backup_mgr.get_backup_summary()
print(f"活跃数据源: {summary['active_source']}")
print(f"健康数据源: {summary['healthy_sources']}")
print(f"本地备份文件数: {summary['local_backup']['total_files']}")

# 查看故障转移历史
history = backup_mgr.get_failover_history(limit=5)
for event in history:
    print(f"{event['timestamp']}: {event['from_source']} -> {event['to_source']}")
```

---

## 常见操作示例

### 示例1：筛选高质量科技股

```python
from core.stock_screener import (
    get_data_source_manager,
    get_industry_screener,
    get_financial_screener,
    get_comprehensive_scorer,
)

# 1. 获取数据
manager = get_data_source_manager()
result = manager.get_mainboard_stocks()
stocks_df = result.data

# 2. 行业筛选
industry_screener = get_industry_screener()
tech_stocks = industry_screener.screen(stocks_df)
print(f"科技股数量: {len(tech_stocks)}")

# 3. 财务筛选
financial_screener = get_financial_screener()
qualified_stocks = financial_screener.screen(tech_stocks)
print(f"财务合格数量: {len(qualified_stocks)}")

# 4. 综合评分
scorer = get_comprehensive_scorer()
scored_stocks = []
for _, stock in qualified_stocks.iterrows():
    score = scorer.score_stock(stock.to_dict())
    scored_stocks.append({
        'code': stock['code'],
        'name': stock['name'],
        'score': score.total_score,
        'rating': score.rating,
    })

# 5. 排序输出
scored_stocks.sort(key=lambda x: x['score'], reverse=True)
print("\n前10名科技股:")
for i, stock in enumerate(scored_stocks[:10], 1):
    print(f"{i}. {stock['code']} {stock['name']} - {stock['score']:.1f}分 ({stock['rating']})")
```

### 示例2：验证数据一致性

```python
from core.stock_screener import (
    get_data_source_manager,
    get_multi_source_validator,
)

# 获取多个数据源的数据
manager = get_data_source_manager()

# 从不同数据源获取数据
sources = {}
for source_type in manager.get_enabled_sources():
    result = manager._sources[source_type].get_all_stocks()
    if result.success:
        sources[source_type.value] = result.data

# 执行多源交叉验证
validator = get_multi_source_validator()
reports = validator.validate_all_sources(sources)

# 输出验证结果
summary = validator.generate_summary_report(reports)
print(summary)
```

### 示例3：监控股票池风险

```python
from core.stock_screener import get_risk_assessor, get_alert_manager
from config.tech_stock_pool import get_tech_stock_pool

# 获取当前股票池
pool = get_tech_stock_pool()
stocks = pool.get_all_stocks()

# 构建风险评估数据
pool_data = [
    {
        'code': stock.code,
        'industry': stock.sector,
        'weight': 1.0 / len(stocks),  # 等权重
    }
    for stock in stocks
]

# 评估风险
assessor = get_risk_assessor()
result = assessor.assess_pool_risk(pool_data)

print(f"股票池风险评估")
print(f"=" * 40)
print(f"风险等级: {result.risk_level.value}")
print(f"行业集中度: {result.metrics.industry_concentration:.1f}%")
print(f"最大单股权重: {result.metrics.max_stock_weight:.1f}%")

if result.warnings:
    print(f"\n风险预警:")
    for warning in result.warnings:
        print(f"  ⚠️ [{warning.risk_type.value}] {warning.message}")
```

---

## 注意事项

### ⚠️ 重要提醒

1. **数据更新频率**
   - 建议每周更新一次股票池
   - 财务数据按季度更新
   - 行情数据每日更新

2. **筛选结果仅供参考**
   - 系统筛选是辅助工具，不是投资建议
   - 请结合自己的研究判断
   - 投资有风险，入市需谨慎

3. **风险控制**
   - 单一行业占比不超过25%
   - 单只股票占比不超过5%
   - 定期检查风险指标

4. **数据质量**
   - 关注数据准确率报告
   - 发现异常及时处理
   - 使用交叉验证确保数据可靠

### 🔧 故障排除

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| 数据获取失败 | 网络问题或数据源维护 | 等待重试或切换数据源 |
| 筛选结果为空 | 筛选条件过严 | 适当放宽筛选标准 |
| 评分异常 | 数据不完整 | 检查数据完整性 |
| 交叉验证失败 | 数据源差异大 | 检查数据源状态 |

### 📚 相关文档

- [新手实操指南](新手实操指南.md) - 系统整体使用指南
- [科技股筛选用户手册](tech_stock_screener_user_manual.md) - 详细功能说明
- [快速参考卡](tech_stock_screener_quick_reference.md) - 常用命令速查
- [故障排除指南](tech_stock_screener_troubleshooting.md) - 问题解决方案

---

## 📞 获取帮助

如果遇到问题：

1. 查看 `logs/` 目录下的运行日志
2. 参考 `docs/` 目录下的详细文档
3. 检查数据源健康状态
4. 验证数据准确率

---

*文档版本: v1.0*
*更新日期: 2026-01-05*
*适用系统版本: MiniQuant-Lite 科技股池扩充版*
